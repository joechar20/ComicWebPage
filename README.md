# ComicWebPage
Uses Github actions to scrape comics daily 
https://joechar20.github.io/ComicWebPage/

## Comics currently scraped
Comic Title, Comic URL
 - andertoons, 'https://www.gocomics.com/andertoons/'  
 - babyblues, 'https://www.gocomics.com/babyblues/'
 - bc, 'https://www.gocomics.com/bc/'
 - beetlebailey, 'https://comicskingdom.com/beetle-bailey-1/'
 - bleeker, 'https://www.gocomics.com/bleeker/'
 - blondie, 'https://www.comicskingdom.com/blondie/'
 - born-loser, 'https://www.gocomics.com/the-born-loser/'
 - calvinandhobbes, 'https://www.gocomics.com/calvinandhobbes/'
 - carpediem, 'https://comicskingdom.com/carpe-diem/' 
 - dennisthemenace, 'https://comicskingdom.com/dennis-the-menace/' 
 - dogsofckennel, 'https://www.gocomics.com/dogsofckennel/'
 - familycircus, 'https://comicskingdom.com/family-circus/'  
 - fminus, 'https://www.gocomics.com/fminus/'
 - frazz, 'https://www.gocomics.com/frazz/'
 - garfield, 'https://www.gocomics.com/garfield/'
 - hagarthehorrible, 'https://comicskingdom.com/hagar-the-horrible/'
 - looseparts, 'https://www.gocomics.com/looseparts/'
 - overthehedge, 'https://www.gocomics.com/overthehedge/'
 - peanuts, 'https://www.gocomics.com/peanuts/'
 - pearlsbeforeswine, 'https://www.gocomics.com/pearlsbeforeswine/'
 - pickles, 'https://www.gocomics.com/pickles/'
 - wizardofid, 'https://www.gocomics.com/wizardofid/'
 - zitscomics, 'https://comicskingdom.com/zits/'

---

## GitHub Pages Comic Reader

Static comic reader hosted on GitHub Pages. Images are pulled in real time from the source sites — nothing is stored in this repo except daily image URL manifests generated by GitHub Actions.

### Configuration — comics.json

All reader settings live in `comics.json` at the repo root. Edit this file and push to apply changes. The next GitHub Actions run picks up any comic list changes automatically.

#### Sort Order (`default_sort`)

**File:** `comics.json` — edit the `"default_sort"` key at the top level of the file (line 3, directly after `"prefetch_days"`).

Controls the order comics appear on the dashboard and the order they are read through.

```json
"default_sort": ["orientation", "title", "date"]
```

| Value | Effect |
|---|---|
| `"orientation"` | Portrait comics first, then landscape |
| `"title"` | Alphabetical by comic title (A → Z) |
| `"date"` | Oldest unread date first |

Default: `["orientation", "title", "date"]` — portrait comics A→Z by oldest unread date, then landscape comics A→Z by oldest unread date.

Change the order or remove entries to customize. Example — title only: `["title", "date"]`

#### Pre-fetch (`prefetch_days`)

**File:** `comics.json` — edit the `"prefetch_days"` key at the top level of the file (line 2, directly after the opening `{`).

Controls how many days ahead the reader silently loads in the background for smooth scrolling.

```json
"prefetch_days": 3
```

| Value | Effect |
|---|---|
| `1` | Only the next day is pre-loaded |
| `3` | Default — next 3 days pre-loaded (recommended) |
| `5` | Larger buffer, uses more data on mobile |

Higher values improve scroll smoothness but increase background data usage. Recommended range: 2–5.

#### Manifest Retention (`MANIFEST_KEEP_DAYS`)

**File:** `scrape_manifest.py` — edit the `MANIFEST_KEEP_DAYS` variable near the top of the file.

Controls how many daily files in the `manifest/` folder are retained before cleanup deletes older date-stamped files.

```python
MANIFEST_KEEP_DAYS = 90
```

How it works:
- After each scrape run, cleanup removes files older than today minus `MANIFEST_KEEP_DAYS`.
- `manifest/latest.json` is not date-stamped and is not affected by this cleanup.

If you want to keep more history, increase the value. Example: `MANIFEST_KEEP_DAYS = 180`.

### GoComics Scrape Fallback (Manifest Reliability)

When GoComics page scraping is blocked by anti-bot checks, the scraper now attempts proxy-based parsing and then falls back to the most recent manifest URL for that comic (up to 7 days back).

This keeps daily manifest entries from becoming `null` during temporary upstream blocking.

#### Adding a Comic

**File:** `comics.json` — add an entry inside the `"comics": [ ]` array, which starts after the top-level settings keys.

Add an entry to the `comics` array in `comics.json` keep them in alphabetical order:

GoComics, landscape:
```json
{
  "id": "garfield",
  "title": "Garfield",
  "url": "https://www.gocomics.com/garfield/",
  "source": "gocomics",
  "orientation": "landscape"
}
```

Comics Kingdom, portrait:
```json
{
  "id": "familycircus",
  "title": "Family Circus",
  "url": "https://comicskingdom.com/family-circus/",
  "source": "comicskingdom",
  "orientation": "portrait"
}
```

- `source`: `"gocomics"` or `"comicskingdom"`
- `orientation`: `"portrait"` or `"landscape"` (affects sort and display)

#### Removing a Comic

**File:** `comics.json` — find and delete the `{ }` block for the comic inside the `"comics": [ ]` array, then push.

Delete its entry from the `comics` array in `comics.json` and push. The comic disappears from the dashboard. Old manifest data is retained but unused.

### Reader Header Format

The reader displays: **`Comic Title : YYYY-MM-DD`**

Example: `Garfield : 2026-08-10`
